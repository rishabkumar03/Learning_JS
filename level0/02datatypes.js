"use strict"; // treat all JS code as newer version
/*

- Prevents us from using undeclared variables.
- Stops us from using unsafe keywords ("with, eval" in unsafe ways).
- Throws more errors instead of silently failing (helps in catching bugs early)
- Reserve certain keywords for future javascr

 */


//alert (3+3) // we are using nodejs, not any browser
/*

- alert() is a part of web browser APIs, not a part of core JavaScript language.
- In browser, it is used to show pop-up message to the user.
- Node.js runs JavaScript outside the browser, that's why it doesn't have windows or alert box.

*/

console.log(3
    +
    3
) // Code readability should be high

console.log("6kku")

// number => 2 to power 4
// bigint (Yhi use krna hai mujhe hackathon mein)
// string => ""
// boolean => true/false
// null => standalone value
// undefined =>
// symbol => unique

// object

console.log(typeof undefined); // undefined
console.log(typeof null); //object
